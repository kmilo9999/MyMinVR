add_library( 
Common INTERFACE
)

target_include_directories(Common INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}")

target_link_libraries(Common
INTERFACE  MinVR::MinVR
)

install(FILES common.h   DESTINATION ${CMAKE_INSTALL_PREFIX}/common/include)
